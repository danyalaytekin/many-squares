const assert = require('assert');
// const shapeBuilder = require('../src/ui/shape-builder');

describe('Shape builder', function () {
    describe('#setRandomPosition(element)', function() {
        beforeEach(function () {
            this.element = {
                style: {}
            };
        });
        
        it('should set the element\'s left style', function () {
            assert.ok (false);
        });

        it('should set the element\'s right style', function () {
            assert.ok (false);
        });
    });
});
